<%= form_with(model: greeting, id: "#{dom_id(greeting)}_form") do |form| %>
  <% if greeting.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(greeting.errors.count, "error") %> prohibited this greeting from being saved:</h2>

      <ul>
        <% greeting.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, class: "form-control form-control-sm"%>
  </div>

  <div class="mb-3">
    <%= form.label :message, style: "display: block" %>
    <%= form.text_area :message, class: "form-control form-control-sm" %>
  </div>

  <div>
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>
